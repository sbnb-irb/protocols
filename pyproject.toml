[project]
name = "protocols"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "sayalaruano", email = "s_ayalaruano@proton.me" }
]
requires-python = ">=3.9"
dependencies = [
    "jupytext>=1.19.5",
]

[project.scripts]
protocols = "protocols:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
